FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

# Source is mounted as a volume for hot-reload
EXPOSE 5173

# --host makes Vite listen on 0.0.0.0 so it's reachable from outside the container
CMD ["npm", "run", "dev", "--", "--host"]

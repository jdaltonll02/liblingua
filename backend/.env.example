# This file is intentionally minimal.
# All environment variables are defined in the root .env.example at:
#   /datasetbuilder/.env.example
#
# Docker Compose reads the root .env and injects every variable
# into the backend container — no backend-specific .env needed.
